# i2-tasks.md — Search Results View

## Tasks

- [ ] T01 — DEV — `GET /searches/{id}` route: query listings + latest price_point + last scan; compute badge for each row
- [ ] T02 — DESIGN — `templates/searches/results.html`: table with all columns, sortable headers via Alpine.js, header with scan info, empty states
- [ ] T03 — DESIGN — Home page: search name in first column links to `/searches/{id}`
